import 'package:flutter/material.dart';

/** 主色调，蓝色 */
const Color MainBlueColor = Color.fromRGBO(44, 109, 210, 1);
/** 主标题，正文，褐色 */
const Color MainTitleColor = Colors.black;
/** 二级标题，主题，深灰色 */
const Color SubTitleColor = Color.fromRGBO(85, 85, 85, 1);
/** 辅助文字、待编辑、失效状态，浅灰 */
const Color MainTextColor = Color.fromRGBO(162, 162, 162, 1);
/** 分割线，灰色 */
const Color LineColor = Color.fromRGBO(224, 224, 224, 1);
/**背景色，浅灰 */
const Color BackgroundColor = Color.fromRGBO(245, 245, 245, 1);
/** 警告，橘红 */
const Color WarningColor = Color.fromRGBO(231, 72, 10, 1);
/** 已完成，浅绿色 */
const Color SuccessColor = Color.fromRGBO(97, 204, 162, 1);
/** 未完成，棕黄色 */
const Color LoadingColor = Color.fromRGBO(238, 158, 71, 1);
/** 未完成，等待，灰色 */
const Color WaitingColor = Color.fromRGBO(169, 169, 169, 1);
/** 超时，错误提示，浅橘红 */
const Color ErrorColor = Color.fromRGBO(237, 107, 71, 1);
/** 正常，已进行，蓝色 */
const Color NormalColor = Color.fromRGBO(10, 142, 255, 1);

const Color DarkColor = Color.fromRGBO(188, 188, 188, 1);